target/
!.mvn/wrapper/maven-wrapper.jar
*.class
*.log

# IDE
.idea/
*.iml
.vscode/
.settings/
.project
.classpath

# Local env (never commit secrets)
.env
infra/.env

# JWT signing keys — generated locally per machine; never commit (prod uses env vars)
conddo-api/src/main/resources/keys/*.pem

# OS
.DS_Store
